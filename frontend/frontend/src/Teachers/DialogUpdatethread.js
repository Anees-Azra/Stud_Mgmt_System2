import React from 'react';
//import CourseList from './CourseList';
import UpdateThreadList from './UpdateThreadList';

const Dialogthread = () => {
  return (
    <UpdateThreadList />
  )
}

export default Dialogthread

